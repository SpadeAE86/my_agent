# infra/scheduler/queue_worker.py — 优先队列异步 Worker
# 职责:
#   1. 维护一个按优先级排序的任务队列
#   2. 由独立线程每 30 分钟触发一次心跳
#   3. 心跳时按优先级依次消费队列中的任务
#   4. 每次心跳设定:
#      - 最大处理任务数 (防止单次处理过多)
#      - 最大可用 token 量 (防止过度消耗 API 额度)
#   5. 支持任务重试与死信队列
