# utils/time_utils.py — 时间相关工具函数
# 函数:
#   now_iso()          — 返回当前时间的 ISO 8601 字符串
#   now_timestamp()    — 返回当前 Unix 时间戳
#   format_date(dt)    — 格式化为 YYYY-MM-DD
#   format_datetime(dt)— 格式化为 YYYY-MM-DD HH:MM:SS
#   parse_datetime(s)  — 解析时间字符串为 datetime 对象
