# utils/id_utils.py — ID 生成工具函数
# 函数:
#   gen_uuid()      — 生成 UUID v4 字符串
#   gen_short_id()  — 生成 8 位短 ID (base62)
#   gen_session_id()— 生成带时间前缀的会话 ID
#   gen_agent_id()  — 生成带角色标识的 Agent ID (main-xxx / sub-xxx)
